import Ember from 'ember';

export default Ember.Component.extend({
  // questionRating: Ember.computed('answer', function() {
  //   var total = 0;
  //   for (var i = 0; i < this.get('answer.length'); i++) {
  //     total += this.get('answer')[i].get('rating');
  //   }
  //   console.log (total);
  //   return total;
  // })

});
