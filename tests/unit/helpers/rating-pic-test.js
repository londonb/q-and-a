import { ratingPic } from 'q-and-a/helpers/rating-pic';
import { module, test } from 'qunit';

module('Unit | Helper | rating pic');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingPic([42]);
  assert.ok(result);
});
